@page
@model ExternalLoginModel
@{
	ViewData["Title"] = "Register";
}

<div class="site-content-warp" style="padding-left: 0">
	<div class="page-section">
		<h1 class="text-center">@ViewData["Title"]</h1>
		<p></p>
		<p></p>
		<h2 class="text-center" id="external-login-title">Associate your @Model.ProviderDisplayName account.</h2>
		<p></p>
		<p></p>
		<p></p>
		<p></p>
		<div class="row d-flex align-items-center justify-content-center">
			<div class="col-md-4">
				<form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
					<div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
					<div class="form-floating mb-3">
						<input asp-for="Input.Email" class="form-control" autocomplete="email" placeholder="Please enter your email." />
						<label asp-for="Input.Email" class="form-label"></label>
						<span asp-validation-for="Input.Email" class="text-danger"></span>
					</div>
					<button type="submit" class="w-100 btn btn-lg btn-primary" style="background-color: #d8c269">Register</button>
				</form>
			</div>
		</div>
	</div>
</div>

@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}
